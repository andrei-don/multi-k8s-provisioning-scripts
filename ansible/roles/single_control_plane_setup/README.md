single_control_plane_setup
=========

single_control_plane_setup bootstraps the control node with kubeadm, installs Calico CNI on the cluster and copies the kubeadm join command on the Ansible controller.


